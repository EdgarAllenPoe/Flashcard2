<Window
    x:Class="FlashcardApp.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlashcardApp.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{ThemeResource SystemAccentColor}" Padding="20,10">
            <TextBlock x:Name="HeaderText" 
                       Text="Flashcard App - Windows Native" 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Main Content Area -->
        <ScrollViewer x:Name="MainScrollViewer" 
                      Grid.Row="1" 
                      Padding="20"
                      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel x:Name="MainPanel" Spacing="10">
                <TextBlock x:Name="OutputTextBlock" 
                           TextWrapping="Wrap" 
                           FontFamily="Consolas"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Input Area -->
        <Border Grid.Row="2" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="InputTextBox" 
                         Grid.Column="0"
                         PlaceholderText="Type your input here..."
                         FontFamily="Consolas"
                         FontSize="14"
                         Margin="0,0,10,0"/>
                
                <Button x:Name="SendButton" 
                        Grid.Column="1"
                        Content="Send"
                        Click="SendButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
